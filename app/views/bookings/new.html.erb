<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <h1>New booking</h1>

      <%= simple_form_for [@devices, @bookings] do |f| %>
  <%= f.input :content %>
  <%= f.submit "book this device", class: "btn btn-primary" %>
<% end %>

    </div>
  </div>
</div>
